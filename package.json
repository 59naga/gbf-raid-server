{
  "private": true,
  "main": "index.mjs",
  "scripts": {
    "start": "node --experimental-modules index.mjs",
    "test": "node --experimental-modules client.mjs"
  },
  "dependencies": {
    "gbf-raid-server": "^0.0.4",
    "socket.io": "^2.1.1"
  },
  "devDependencies": {
    "socket.io-client": "^2.1.1"
  },
  "engines": {
    "node": "10.6.0",
    "yarn": "1.8.0"
  }
}
